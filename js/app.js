/* ==========================================================
   PROJECTE ORIGEN
   APP.JS
========================================================== */

document.addEventListener("DOMContentLoaded", async () => {

    await loadAgents();

    resetTerminal();

    showIncomingCall();

});